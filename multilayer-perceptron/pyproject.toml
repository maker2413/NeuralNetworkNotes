[project]
name = "multilayer-perceptron"
version = "0.1.0"
description = "Studying multilayer perceptrons"
readme = "README.org"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.9.2",
    "numpy>=2.1.3",
    "torch>=2.5.1",
]
